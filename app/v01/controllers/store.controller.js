var QRCode = require('qrcode')

function createQr(){

    /*
    QRCode.toDataURL('I am a pony!', function (err, url) {
      console.log(url)
    })

    QRCode.toFile(__dirname  + "/test.svg", 'https://www.google.de', function (err, url) {
        console.log(url)
      })
      */

}

module.exports =   { createQr };